#include <stdio.h>
#include "data.h"

// 데이터 파일로부터 초기 데이터 목록을 로딩
void load_data_from_file(Item *items, int *num_items) {
    // 파일에서 데이터를 읽어와 items 배열에 저장하는 코드
    // 이 예시에서는 단순히 초기화만 수행
    *num_items = 0;
}

// 현재 변경된 데이터 목록을 데이터 파일로 저장
void save_data_to_file(Item *items, int num_items) {
    // 변경된 데이터 목록을 데이터 파일에 저장하는 코드
}
