#include <stdio.h>
#include "data.h"

void load_data_from_file(Game *games, int *num_games) {
    // 파일에서 데이터를 읽어와 games 배열에 저장하는 코드
    // 이 예시에서는 단순히 초기화만 수행
    *num_games = 0;
}

void save_data_to_file(Game *games, int num_games) {
    // 데이터를 파일에 저장하는 코드
}
